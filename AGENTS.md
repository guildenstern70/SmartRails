# AGENTS.md

## Basic features
- Ruby on Rails 8 application
- Uses Ruby v.4
- Uses Bulma as CSS framework (already installed and configured)

## Unit test
- Always update and run tests after editin (bin/rails test)

## Code style
- Use RuboCop style (bundle exec rubocop)
- Prefer double-quoted strings unless you need single quotes to avoid extra backslashes for escaping